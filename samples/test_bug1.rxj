#!/usr/bin/env rexx
/* ========================================================================
   JDORFX Test winFrame issue
   ======================================================================== */

-- create JDORFX handler
-- load and add the Java Rexx command handler, using default name: JDORFX
call addJdorFXHandler
-- set default environment to JDORFX
useRedirection? = .true
if useRedirection? then
    address jdorfx with output using (.stdout) error using (.stderr)
else
    address jdorfx

newImage 600 600
winTitle "JDORFX"

-- Create a circle
color red
shape circle ELLI 175 175 250 250

-- Create fade animation (fade in and out indefinitely)
-- Parameters:
--   animName, nodeName, duration, fromValue, toValue, cycleCount, autoReverse
animationFade fadeAnim circle 2000 0.1 1.0 10 .true
animationPlay fadeAnim

winShow
fillShape circle

sleep 3

winFrame .false

sleep 3
winFrame .true

say "-- Circle fading in and out..."
say "-- Press Enter to stop and exit..."
if useRedirection? then
    sleep 5
else
    pull .

animationStop fadeAnim
winClose

say "-- Animation completed!"

exit 0

-- get ooRexx-Java bridge, contains JDORFX Rexx command handler
::requires "jdorfx.cls"
